# Matlab — Phase 2 evaluations

GRiFFIN-style Phase-1 + Phase-2 simulations and plots (static threshold vs SAC dynamic threshold).

## Folders

- `Code for the exact six Griffin test scenarios/`
- `Code with OnOFF attacks/`
- `Code with same six test but Gradual Activation/`
- `Other Plotting such as balance accuracy/`

Each simulation folder includes `sacActorPredictMATLAB.m` and `sac_actor_export.mat`.

Update hard-coded `projectDir` / `resultsDir` paths before running. See [`../TECHNICAL.md`](../TECHNICAL.md) for the full pipeline.
